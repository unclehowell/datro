# RULES — COMMAND Cockpit

1. Graph view is the DEFAULT view (road hidden behind toggle)
2. All API calls must include CORS headers
3. No external dependencies beyond d3.js and built-in browser APIs
4. All state must survive page reload (localStorage for UI, KV for server state)
5. Free tier only — no paid Cloudflare or OpenAI services
